# MLB Database Configuration
# Copy this file to .env and update with your actual database credentials

# Database host (usually localhost for local development)
# For production, this might be a remote MySQL server
MLB_DB_HOST=localhost

# Database port (default MySQL port is 3306)
MLB_DB_PORT=3306

# Database name (should contain 'dw' schema with analysis tables)
MLB_DB_NAME=mlb

# Database user (requires READ access to 'dw' schema)
MLB_DB_USER=your_user

# Database password
MLB_DB_PASS=your_pass

# Optional: Full DATABASE_URL format (alternative to individual vars above)
# DATABASE_URL=mysql://user:pass@host:3306/mlb
